machine:
    timezone:
        Asia/Tokyo

deployment:
    production:
        branch: master
        commands:
            - export VALUE=HOGE
            - echo "Hello! ${HOGE}"
            - VALUE=HOGE echo "Hello! ${HOGE}"
