machine:
    timezone:
        Asia/Tokyo

deployment:
    production:
        branch: master
        commands:
            - export VALUE=HOGE
            - echo "Hello! ${VALUE}"
            - VALUE=HOGE echo "Hello! ${VALUE}"
